# Server
PORT=3001
MANAGER_PASSWORD=change-me

# Optional: public join URL used for QR codes.
# Leave empty to auto-detect from incoming request host/protocol.
JOIN_BASE_URL=

# Logging (optional)
# LOGGING=1 enables general server logs
# CAMOOT_PLAYER_DEBUG=1 enables extra player-answer debug logs
LOGGING=1
CAMOOT_PLAYER_DEBUG=1
